GradeApp с GUI (Swing) 

Това е малък Java проект за управление на оценки на студенти с GUI интерфейс, написан за JDK 1.8.Проектът използва няколко дизайн патърна (Observer, Proxy, Factory) и е разделен на отделни класове.

📁 Структура на проекта

Файл - Описание:
Student.java - Модел за студент с id, name и group.
Discipline.java - Модел за дисциплина (предмет) с име.
Grade.java - Модел за оценка, съдържа studentId, Discipline, стойност и timestamp.
StudentRepository.java - Интерфейс за достъп до студенти (addStudent, findById, findByGroup, getAllStudents).
RealStudentRepository.java - Реално хранилище за студенти, имитира забавяне при заявки.
StudentRepositoryProxy.java - Proxy за репозитория — кешира getAllStudents и логва повикванията.
GradeService.java - Управление на оценки; поддържа наблюдатели (Observer pattern).
DisciplineFactory.java - Фабрика за създаване на дисциплини (може да се разширява за elective/mandatory предмети).
GroupView.java - Observer за група — визуализира оценки на студенти от дадена група; поддържа callback за GUI.
MainWindow.java - Главният GUI прозорец: таблица със студенти, филтри, форма за добавяне на оценки и панел за автоматичен GroupView.
StudentsTableModel.java - TableModel за JTable с колони: ID, Име, Група.
SimpleDocumentListener.java - Помощен интерфейс за слушане на промени в текстови полета.


🛠 Дизайн и патърни

1. Observer Pattern
o GradeService държи списък от наблюдатели (GradeObserver).
o GroupView се регистрира като наблюдател и се нотифицира при добавяне на оценка.
o GUI панелът за група се обновява автоматично.

2. Proxy Pattern
o StudentRepositoryProxy кешира резултата на getAllStudents() и логва повикванията.
o Помага за оптимизация и дебъгване.

3. Factory Pattern
o DisciplineFactory създава дисциплини и предоставя списък с предварително дефинирани предмети.


⚙ Инструкции за компилация и стартиране

# Компилация на всички файлове
javac *.java

# Стартиране на приложението
java GradeAppWithGUI
Всички класове трябва да са в един пакет или в default package. Кодът е тестван на JDK 1.8.


🖥 GUI функционалности:
• Таблица със студенти: показва ID, име и група.
• Филтри: по име и група, с опция за изчистване.
• Форма за добавяне на оценка:
o Избор на студент от ComboBox.
o Избор на дисциплина.
o Въвеждане на стойност (2.00–6.00).
• Group view: панел, който автоматично показва всички студенти от дадена група с техните оценки, веднага след като се добави нова оценка.

📌 Забележки
• Кодът е пренаписан да е съвместим с Java 1.8 — използва стандартни null-стойности вместо Optional, lambda функции са използвани само в JDK 1.8-стил.
• Всички операции по GUI се изпълняват на Event Dispatch Thread чрез SwingUtilities.invokeLater.
• Proxy и Observer са основните места за демонстрация на дизайн патърни.

🔧 Бъдещи подобрения
• Добавяне на сортиране по колони в таблицата.
• Поддръжка на различни видове дисциплини (elective/mandatory) чрез фабриката.
• Подобряване на визуализацията на групови оценки (цветове, графики).

Това е минималистичен, но напълно функционален Swing проект за управление на оценки.

